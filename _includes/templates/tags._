<ul>
  <% _.each( tags, function(tag) { %>
    <li class="tag" id="tag-<%= tag.id %>"><%= tag.name %></li>
  <% }) %>
  <li>
</ul>
<form id="search_form">
  <input type="text" id="tag_list" placeholder="Tag this page..." value="<%= tag_list %>" />
</form>